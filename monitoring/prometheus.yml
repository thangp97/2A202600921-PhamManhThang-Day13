global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: day13-observability-lab
    metrics_path: /metrics/prometheus
    static_configs:
      # App chạy trên host (uvicorn :8000); host.docker.internal trỏ từ container về host.
      - targets: ["host.docker.internal:8000"]
